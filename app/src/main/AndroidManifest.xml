<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for photo attachment -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketPlan">
        <!-- Splash Activity - Entry Point -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PocketPlan.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.PocketPlan.NoActionBar" />

        <!-- Register Activity -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.PocketPlan.NoActionBar" />


        <!-- Categories Activity -->
        <activity
            android:name=".ui.categories.CategoriesActivity"
            android:exported="false" />

        <!-- Add Expense Activity -->
        <activity
            android:name=".ui.expenses.AddExpenseActivity"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Dashboard Activity -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false" />


        <!-- Analytics Activity -->
        <activity
            android:name=".ui.analytics.AnalyticsActivity"
            android:exported="false" />


        <!-- Category Spending Activity -->
        <activity
            android:name=".ui.spending.CategorySpendingActivity"
            android:exported="false" />


        <!-- View Expenses List Activity -->
        <activity
            android:name=".ui.expenses.ExpensesListActivity"
            android:exported="false" />

        <!-- Budget Goals Activity -->
        <activity
            android:name=".ui.budget.BudgetGoalsActivity"
            android:exported="false" />

        <!-- Achievements Activity -->
        <activity
            android:name=".ui.gamification.AchievementsActivity"
            android:exported="false" />


        <!-- Shared Budgeting Activity -->
        <activity
            android:name=".ui.shared.SharedBudgetActivity"
            android:exported="false" />

        <activity
            android:name=".ui.shared.SharedBudgetDetailsActivity"
            android:exported="false" />

        <!-- Loan Planner Activity -->
        <activity
            android:name=".ui.loans.LoanPlannerActivity"
            android:exported="false" />

        <activity
            android:name=".ui.loans.LoanDetailsActivity"
            android:exported="false" />

    </application>

</manifest>